using Microsoft.AspNetCore.Identity;

namespace PropertyManagement.API.Models
{
    // role C user type with full access to all system features
    // this class is intentionally empty because all fields come from IdentityUser
    public class PropertyManager : IdentityUser
    {

    }
}
